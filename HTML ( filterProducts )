<template>
    <lightning-card variant="Narrow"  title="SHOPPING FILTER" icon-name="action:new_note">
        <hr>
        <lightning-combobox
            name="progress"
            label="Choose a Category"
            value={category}
            placeholder="Select Progress"
            options={options}
            onchange={handleChangeCategory} ></lightning-combobox>
            <hr>
        <lightning-combobox
            name="progress1"
            label="Choose a Color"
            value={color}
            placeholder="Select Progress"
            options={options1}
            onchange={handleChangeColor} ></lightning-combobox>
            <hr>
       <lightning-button variant="brand-outline" label="Filter" title="Primary action with lighter look" onclick={handleClick} class="slds-m-left_x-small"></lightning-button>
    </lightning-card>
</template>
